# kat-rs 工作协议

团队优先追求有效信息密度、可验证结果和可 review 的小步交付，不追求想象中的未来扩展性。

## 0. AGENTS.md 的上下文预算

`AGENTS.md` 是会进入人和 AI 常驻上下文的高优先级文件。每新增一句都必须证明它极为必要：长期有效、跨任务高频触发、需要动手前判断，并且无法由更局部的模板、CI、脚本、普通文档或代码注释更好承载。

不要把阶段性讨论、历史解释、阈值、标签、脚本行为、局部实现细节或一次性要求写进这里。

## 1. 先想清楚，再动手

非平凡变更从 issue 和轻量 SDD 开始。动手前必须能说明：要解决什么问题、明确不做什么、最小可交付切片是什么、如何验证。说不清时，先澄清，不写代码。

## 2. 简单优先

只实现当前问题需要的最小切片。不为想象中的未来加抽象、配置项、扩展点或交付面。

通用抽象优先使用 Rust 标准库和成熟社区 crate；项目代码只保留领域逻辑和必要胶水，不重建社区已有的解析、序列化、查询、格式转换等基础设施。自实现通用能力前，必须在 issue/SDD 或 PR 中说明现成方案为何不适用。

## 3. 外科手术式改动

只修改本次目标需要的文件。不要顺手重构无关代码，不要格式化无关文件，不要删除与本次变更无关的旧代码。发现无关问题时，在 PR 或 issue 中说明，不在当前 PR 里扩大范围。

## 4. 交付 PR 只放交付内容

交付 PR 只放本次最小切片需要的交付内容。临时探索、AI 中间产物、生成物、未验证能力或未经 issue/SDD 批准的交付面，不属于交付内容。

## 5. 验证证据

动手前先想清楚如何验证。PR 中必须给出实际验证证据；没有验证证据的 PR 不应进入 review。

## 6. 中文优先

issue、PR、设计文档、review 说明和非显然注释中文优先。代码标识符、公共 API、命令、crate 和模块名保持英文。注释只解释业务意图、架构取舍、边界约束或不容易从代码看出的原因，不给显然代码加翻译式注释。

# PACK 创作与维护流程入口

完整正文由已安装 SDK 维护。先按 [SDK 安装与知识流程](../../kat/references/sdk-install.md) 确认共享 Python，再用其绝对路径读取：

```text
<python> -I -X utf8 -c "from kat import knowledge; print(knowledge.read('authoring'))"
```

按返回正文执行当前任务；源码位置为 `kat/sdk/kat/knowledge/authoring/pack-authoring-flow.md`。读取失败时按 SDK 安装流程修复，不以另一版本的文档替代。

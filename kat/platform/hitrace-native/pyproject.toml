[build-system]
requires = ["maturin==1.14.1"]
build-backend = "maturin"

[project]
name = "kat-hitrace-native"
requires-python = ">=3.14,<3.15"
dynamic = ["version"]

[tool.maturin]
module-name = "_kat_hitrace"
features = ["pyo3/extension-module"]

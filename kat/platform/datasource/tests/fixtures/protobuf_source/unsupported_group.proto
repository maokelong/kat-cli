syntax = "proto2";

package fixture.protobuf_source.unsupported.group_shape;

message GroupReachableRoot {
  optional group LegacyPayload = 1 {
    optional string value = 2;
  }
}

// 绑定该 root 时，group 位于 reachable closure 外，不应阻塞。
message GroupUnreachableRoot {
  optional string value = 1;
}

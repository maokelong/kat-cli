syntax = "proto3";

package fixture.protobuf_source.unsupported.map_shape;

message MapReachableRoot {
  map<string, int32> values = 1;
}

// 绑定该 root 时，上面的 map 位于 reachable closure 外，不应阻塞。
message MapUnreachableRoot {
  string value = 1;
}

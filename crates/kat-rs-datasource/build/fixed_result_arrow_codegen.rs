use std::{env, fmt::Write as _, fs, path::PathBuf};

use crate::{fixed_result_domain_codegen::FixedResultPluginSpec, proto_codegen::camel_to_snake};

const FIXED_RESULT_TABLE_BUILDERS_FILE: &str = "fixed_result_table_builders.rs";
const FIXED_RESULT_TABLE_SET_NAME: &str = "FixedResultTableSet";

pub(crate) fn generate_fixed_result_table_builders(
    specs: &[FixedResultPluginSpec],
) -> std::io::Result<()> {
    let out_dir = PathBuf::from(env::var_os("OUT_DIR").expect("OUT_DIR is set"));
    fs::write(
        out_dir.join(FIXED_RESULT_TABLE_BUILDERS_FILE),
        render_table_builders(specs),
    )
}

fn render_table_builders(specs: &[FixedResultPluginSpec]) -> String {
    let mut output = String::new();
    output.push_str("// @generated by crates/kat-rs-datasource/build.rs. Do not edit.\n\n");
    output.push_str("use anyhow::Result;\n\n");
    output.push_str("use crate::{\n");
    output.push_str("    arrow_table::ArrowTable,\n");
    output.push_str("    domains::fixed_result::FixedResultRecord,\n");
    output.push_str("    proto::kat::{\n");
    for spec in specs {
        writeln!(
            output,
            "        {}::{{{}, {}}},",
            spec.module_name, spec.config_message, spec.result_message
        )
        .expect("write to string");
    }
    output.push_str("    },\n");
    output.push_str("    sinks::arrow::MessageTableBuilder,\n");
    output.push_str("};\n\n");

    writeln!(output, "pub(crate) struct {FIXED_RESULT_TABLE_SET_NAME} {{")
        .expect("write to string");
    for spec in specs {
        writeln!(
            output,
            "    {}: MessageTableBuilder<{}>,",
            camel_to_snake(spec.config_message),
            spec.config_message
        )
        .expect("write to string");
        writeln!(
            output,
            "    {}: MessageTableBuilder<{}>,",
            camel_to_snake(spec.result_message),
            spec.result_message
        )
        .expect("write to string");
    }
    output.push_str("}\n\n");

    writeln!(output, "impl {FIXED_RESULT_TABLE_SET_NAME} {{").expect("write to string");
    output.push_str("    pub(crate) fn new() -> Result<Self> {\n");
    output.push_str("        Ok(Self {\n");
    for spec in specs {
        writeln!(
            output,
            "            {}: MessageTableBuilder::new({:?})?,",
            camel_to_snake(spec.config_message),
            spec.config_table
        )
        .expect("write to string");
        writeln!(
            output,
            "            {}: MessageTableBuilder::new({:?})?,",
            camel_to_snake(spec.result_message),
            spec.result_table
        )
        .expect("write to string");
    }
    output.push_str("        })\n");
    output.push_str("    }\n\n");

    output.push_str(
        "    pub(crate) fn push_record(&mut self, record: FixedResultRecord) -> Result<()> {\n",
    );
    output.push_str("        match record {\n");
    for spec in specs {
        writeln!(
            output,
            "            FixedResultRecord::{}(record) => self.{}.push(*record)?,",
            spec.config_variant(),
            camel_to_snake(spec.config_message)
        )
        .expect("write to string");
        writeln!(
            output,
            "            FixedResultRecord::{}(record) => self.{}.push(*record)?,",
            spec.result_variant(),
            camel_to_snake(spec.result_message)
        )
        .expect("write to string");
    }
    output.push_str("        }\n\n");
    output.push_str("        Ok(())\n");
    output.push_str("    }\n\n");

    output.push_str("    pub(crate) fn into_tables(self) -> Result<Vec<ArrowTable>> {\n");
    output.push_str("        Ok(vec![\n");
    for spec in specs {
        writeln!(
            output,
            "            self.{}.into_table()?,",
            camel_to_snake(spec.config_message)
        )
        .expect("write to string");
        writeln!(
            output,
            "            self.{}.into_table()?,",
            camel_to_snake(spec.result_message)
        )
        .expect("write to string");
    }
    output.push_str("        ])\n");
    output.push_str("    }\n\n");

    output.push_str("    #[allow(dead_code)]\n");
    output.push_str("    pub(crate) fn flush_tables(&mut self) -> Result<Vec<ArrowTable>> {\n");
    output.push_str("        Ok(vec![\n");
    for spec in specs {
        writeln!(
            output,
            "            self.{}.flush_table()?,",
            camel_to_snake(spec.config_message)
        )
        .expect("write to string");
        writeln!(
            output,
            "            self.{}.flush_table()?,",
            camel_to_snake(spec.result_message)
        )
        .expect("write to string");
    }
    output.push_str("        ])\n");
    output.push_str("    }\n");
    output.push_str("}\n");

    output
}

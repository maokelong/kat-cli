# hilog_basic

Placeholder for the first checked-in Hilog golden suite.

Expected layout:

```text
hilog_basic/
  inspect.expected.json
  queries/
    log_count.sql
    log_count.expected.json
  errors/
    malformed_query.sql
    malformed_query.expected.json
```
